# Automação e Aprovação Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar o ciclo gerar → aprovar → publicar: um poller novo que preenche o calendário de conteúdo sozinho, avalia regras de aprovação, e dispara a publicação agendada de pieces já aprovadas — respeitando `entitlement_status`.

**Architecture:** Um único módulo novo, `app/services/content/automation_scheduler.py`, com um loop próprio no lifespan (mesmo padrão de `publish_dispatcher.py`) rodando três passes sequenciais a cada tick. Nenhum código das fases 2 (geração) ou 3 (publicação) é modificado — o scheduler só chama funções públicas já existentes (`pieces_service.create_piece`, `publications_service.resolve_publication_request`). Dois campos de estado novos em `ContentPiece` (`approval_action`, `is_autogenerated`) e uma tabela nova (`content_generation_templates`) sustentam a lógica.

**Tech Stack:** FastAPI, SQLModel/SQLAlchemy, Alembic, Postgres, Python 3.11 — mesmo stack das fases anteriores.

**Spec:** `docs/superpowers/specs/2026-08-29-automacao-aprovacao-design.md`

## Global Constraints

- Nenhuma alteração em `app/services/content/publish_dispatcher.py`, nos adapters de `app/services/content/publishers/`, ou em `app/services/content/pipeline.py`/`orchestrator.py` — este plano só chama funções públicas dessas fases, nunca as modifica.
- `entitlement_status != active` pausa geração proativa (Passe 1) e publicação agendada (Passe 3); nunca bloqueia os endpoints manuais das fases 2/3.
- `requires_human_review=true` sempre vence sobre qualquer `ApprovalRule` — nenhuma regra pode auto-aprovar por cima.
- Nenhuma regra casando (ou nenhuma configurada) resolve para `require_review` — fail-safe é sempre o padrão.
- Todo passe do scheduler roda com queries simples (sem `SKIP LOCKED`); Passes 1 e 3 tratam `IntegrityError` por item (não por batch) para serem seguros com múltiplas réplicas; Passe 2 usa `UPDATE ... WHERE status='pending_approval'` como guarda otimista, mesmo padrão usado nos endpoints manuais de aprovar/rejeitar.
- Toda transição de status grava em `content_audit_logs` (`actor="system:automation_scheduler"` ou `"system:approval_engine"` para ações do motor, `f"tenant:{tenant.id}"` para ações manuais — mesmo padrão já usado em todo o módulo).
- Migração nova encadeia a partir do head atual, `b6a1f9c3d2e7` (última migração da fase 3).

---

### Task 1: Modelo de dados — tabela de templates + campos novos em ContentPiece

**Files:**
- Modify: `app/models/content.py`
- Create: `alembic/versions/e4c2a9f1b6d3_add_automation_approval_tables.py`

**Interfaces:**
- Consumes: `ContentPieceType`, `ContentCategory`, `ApprovalAction` (enums já existentes em `app/models/content.py`).
- Produces: modelo `ContentGenerationTemplate` (tabela `content_generation_templates`); DTOs `GenerationTemplateCreate`, `GenerationTemplateRead`; campos novos `ContentPiece.approval_action: Optional[ApprovalAction]`, `ContentPiece.approved_at: Optional[datetime]`, `ContentPiece.is_autogenerated: bool`. Todas as tasks seguintes dependem destes nomes exatos.

- [ ] **Step 1: Adicionar o modelo `ContentGenerationTemplate` e os campos novos em `ContentPiece`**

Em `app/models/content.py`, adicione os três campos abaixo ao final da classe `ContentPiece` (depois de `updated_at`):

```python
    approval_action: Optional[ApprovalAction] = Field(
        default=None,
        sa_column=Column(SAEnum(ApprovalAction, name="content_approval_action")),
    )
    approved_at: Optional[datetime] = None
    is_autogenerated: bool = Field(default=False)
```

Logo depois da classe `ContentApprovalRule` (antes de `ContentAuditLog`), adicione a tabela nova:

```python
class ContentGenerationTemplate(SQLModel, table=True):
    __tablename__ = "content_generation_templates"

    id: Optional[int] = Field(default=None, primary_key=True)
    campaign_id: int = Field(foreign_key="content_campaigns.id", index=True)
    type: ContentPieceType = Field(
        sa_column=Column(SAEnum(ContentPieceType, name="content_piece_type"))
    )
    generation_prompt: Optional[str] = None
    avatar_id: Optional[int] = Field(default=None, foreign_key="content_avatars.id")
    voice_id: Optional[str] = None
    is_synthetic_media: bool = Field(default=False)
    content_category: Optional[ContentCategory] = Field(
        default=None,
        sa_column=Column(SAEnum(ContentCategory, name="content_category")),
    )
    aspect_ratio: str = Field(default="9:16")
    resolution: Optional[str] = None
    duration: Optional[int] = None
    created_at: datetime = Field(default_factory=datetime.utcnow)
```

Nos DTOs (seção `# --- DTOs ---`), logo depois de `ApprovalRuleRead`, adicione:

```python
class GenerationTemplateCreate(BaseModel):
    campaign_id: int
    type: ContentPieceType
    generation_prompt: Optional[str] = None
    avatar_id: Optional[int] = None
    voice_id: Optional[str] = None
    is_synthetic_media: bool = False
    content_category: Optional[ContentCategory] = None
    aspect_ratio: str = "9:16"
    resolution: Optional[str] = None
    duration: Optional[int] = None


class GenerationTemplateRead(BaseModel):
    id: int
    campaign_id: int
    type: ContentPieceType
    generation_prompt: Optional[str]
    avatar_id: Optional[int]
    voice_id: Optional[str]
    is_synthetic_media: bool
    content_category: Optional[ContentCategory]
    aspect_ratio: str
    resolution: Optional[str]
    duration: Optional[int]
    created_at: datetime
```

- [ ] **Step 2: Verificar que o módulo importa sem erro**

Run: `python -c "from app.models.content import ContentGenerationTemplate, GenerationTemplateCreate, GenerationTemplateRead, ContentPiece; print(ContentPiece.model_fields.keys())"`
Expected: imprime a lista de campos de `ContentPiece` incluindo `approval_action`, `approved_at`, `is_autogenerated`, sem exceção.

- [ ] **Step 3: Escrever a migração Alembic**

Crie `alembic/versions/e4c2a9f1b6d3_add_automation_approval_tables.py`:

```python
"""add automation and approval tables

Revision ID: e4c2a9f1b6d3
Revises: b6a1f9c3d2e7
Create Date: 2026-08-29 12:00:00.000000

"""
from alembic import op
import sqlalchemy as sa
import sqlmodel


# revision identifiers, used by Alembic.
revision = 'e4c2a9f1b6d3'
down_revision = 'b6a1f9c3d2e7'
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        'content_generation_templates',
        sa.Column('id', sa.Integer(), nullable=False),
        sa.Column('campaign_id', sa.Integer(), nullable=False),
        sa.Column(
            'type',
            sa.Enum('video', 'image', 'audio', name='content_piece_type', create_type=False),
            nullable=True,
        ),
        sa.Column('generation_prompt', sqlmodel.sql.sqltypes.AutoString(), nullable=True),
        sa.Column('avatar_id', sa.Integer(), nullable=True),
        sa.Column('voice_id', sqlmodel.sql.sqltypes.AutoString(), nullable=True),
        sa.Column('is_synthetic_media', sa.Boolean(), server_default=sa.false(), nullable=False),
        sa.Column(
            'content_category',
            sa.Enum(
                'medical', 'pharmaceutical', 'financial', 'insurance', 'legal',
                'alcohol', 'gambling', 'political', 'regulated_product',
                name='content_category', create_type=False,
            ),
            nullable=True,
        ),
        sa.Column('aspect_ratio', sqlmodel.sql.sqltypes.AutoString(), server_default='9:16', nullable=False),
        sa.Column('resolution', sqlmodel.sql.sqltypes.AutoString(), nullable=True),
        sa.Column('duration', sa.Integer(), nullable=True),
        sa.Column('created_at', sa.DateTime(), nullable=False),
        sa.ForeignKeyConstraint(['avatar_id'], ['content_avatars.id'], ),
        sa.ForeignKeyConstraint(['campaign_id'], ['content_campaigns.id'], ),
        sa.PrimaryKeyConstraint('id'),
    )
    op.create_index(
        op.f('ix_content_generation_templates_campaign_id'),
        'content_generation_templates', ['campaign_id'], unique=False,
    )

    op.add_column('content_pieces', sa.Column(
        'approval_action',
        sa.Enum('auto_approve', 'require_review', name='content_approval_action', create_type=False),
        nullable=True,
    ))
    op.add_column('content_pieces', sa.Column('approved_at', sa.DateTime(), nullable=True))
    op.add_column('content_pieces', sa.Column(
        'is_autogenerated', sa.Boolean(), server_default=sa.false(), nullable=False,
    ))


def downgrade() -> None:
    op.drop_column('content_pieces', 'is_autogenerated')
    op.drop_column('content_pieces', 'approved_at')
    op.drop_column('content_pieces', 'approval_action')
    op.drop_index(
        op.f('ix_content_generation_templates_campaign_id'),
        table_name='content_generation_templates',
    )
    op.drop_table('content_generation_templates')
```

`create_type=False` em todos os três enums reutilizados é deliberado: `content_piece_type`, `content_category` e `content_approval_action` já foram criados pelas migrações `60a7421f3570`/`574ed629fa1f` — recriá-los aqui quebraria o `upgrade()`.

- [ ] **Step 4: Verificar que a migração vira o novo head, sem heads múltiplos**

Run: `python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; s = ScriptDirectory.from_config(Config('alembic.ini')); print(s.get_heads())"`
Expected: `['e4c2a9f1b6d3']` — uma lista com um único elemento, confirmando que a migração encadeia corretamente a partir de `b6a1f9c3d2e7` e não cria uma branch paralela.

- [ ] **Step 5: Commit**

```bash
git add app/models/content.py alembic/versions/e4c2a9f1b6d3_add_automation_approval_tables.py
git commit -m "feat(content): add generation-template table and approval fields on ContentPiece"
```

---

### Task 2: Serviço de templates de geração (CRUD)

**Files:**
- Create: `app/services/content/generation_templates.py`
- Test: `test/services/test_content_generation_templates.py`

**Interfaces:**
- Consumes: `ContentGenerationTemplate`, `GenerationTemplateCreate` (Task 1); `get_campaign` de `app/services/content/campaigns.py` (assinatura: `get_campaign(session, *, tenant_id, campaign_id) -> Optional[ContentCampaign]`).
- Produces: `create_template(session, *, tenant_id, campaign_id, type, generation_prompt, avatar_id, voice_id, is_synthetic_media, content_category, aspect_ratio, resolution, duration) -> Optional[ContentGenerationTemplate]`, `list_templates(session, *, tenant_id, campaign_id) -> List[ContentGenerationTemplate]`. Usadas pelo controller (Task 3) e por `automation_scheduler.py` (Task 6).

- [ ] **Step 1: Escrever o teste falho**

Crie `test/services/test_content_generation_templates.py`:

```python
import unittest
from unittest.mock import MagicMock, patch

from app.models.content import ContentPieceType
from app.services.content import generation_templates as templates_service


class TestCreateTemplate(unittest.TestCase):
    def test_returns_none_when_campaign_not_found(self):
        session = MagicMock()

        with patch.object(templates_service, "get_campaign", return_value=None):
            result = templates_service.create_template(
                session,
                tenant_id=1,
                campaign_id=99,
                type=ContentPieceType.image,
                generation_prompt="a cat",
                avatar_id=None,
                voice_id=None,
                is_synthetic_media=False,
                content_category=None,
                aspect_ratio="9:16",
                resolution=None,
                duration=None,
            )

        self.assertIsNone(result)
        session.add.assert_not_called()

    def test_creates_and_persists_template_when_campaign_exists(self):
        session = MagicMock()
        campaign = MagicMock(id=5)

        with patch.object(templates_service, "get_campaign", return_value=campaign):
            templates_service.create_template(
                session,
                tenant_id=1,
                campaign_id=5,
                type=ContentPieceType.video,
                generation_prompt="a dog",
                avatar_id=None,
                voice_id="v1",
                is_synthetic_media=True,
                content_category=None,
                aspect_ratio="16:9",
                resolution="1080p",
                duration=10,
            )

        session.add.assert_called_once()
        added = session.add.call_args.args[0]
        self.assertEqual(added.campaign_id, 5)
        self.assertEqual(added.type, ContentPieceType.video)
        self.assertEqual(added.aspect_ratio, "16:9")
        session.commit.assert_called_once()


class TestListTemplates(unittest.TestCase):
    def test_returns_empty_list_when_campaign_not_found(self):
        session = MagicMock()

        with patch.object(templates_service, "get_campaign", return_value=None):
            result = templates_service.list_templates(session, tenant_id=1, campaign_id=99)

        self.assertEqual(result, [])

    def test_returns_templates_for_campaign(self):
        session = MagicMock()
        campaign = MagicMock(id=5)
        rows = [MagicMock(), MagicMock()]
        session.exec.return_value.all.return_value = rows

        with patch.object(templates_service, "get_campaign", return_value=campaign):
            result = templates_service.list_templates(session, tenant_id=1, campaign_id=5)

        self.assertEqual(result, rows)
```

- [ ] **Step 2: Rodar e confirmar falha**

Run: `pytest test/services/test_content_generation_templates.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.services.content.generation_templates'`

- [ ] **Step 3: Implementar o serviço**

Crie `app/services/content/generation_templates.py`:

```python
from typing import List, Optional

from sqlmodel import Session, select

from app.models.content import ContentCategory, ContentGenerationTemplate, ContentPieceType
from app.services.content.campaigns import get_campaign


def create_template(
    session: Session,
    *,
    tenant_id: int,
    campaign_id: int,
    type: ContentPieceType,
    generation_prompt: Optional[str],
    avatar_id: Optional[int],
    voice_id: Optional[str],
    is_synthetic_media: bool,
    content_category: Optional[ContentCategory],
    aspect_ratio: str,
    resolution: Optional[str],
    duration: Optional[int],
) -> Optional[ContentGenerationTemplate]:
    if get_campaign(session, tenant_id=tenant_id, campaign_id=campaign_id) is None:
        return None
    template = ContentGenerationTemplate(
        campaign_id=campaign_id,
        type=type,
        generation_prompt=generation_prompt,
        avatar_id=avatar_id,
        voice_id=voice_id,
        is_synthetic_media=is_synthetic_media,
        content_category=content_category,
        aspect_ratio=aspect_ratio,
        resolution=resolution,
        duration=duration,
    )
    session.add(template)
    session.commit()
    session.refresh(template)
    return template


def list_templates(
    session: Session, *, tenant_id: int, campaign_id: int
) -> List[ContentGenerationTemplate]:
    if get_campaign(session, tenant_id=tenant_id, campaign_id=campaign_id) is None:
        return []
    return list(
        session.exec(
            select(ContentGenerationTemplate).where(
                ContentGenerationTemplate.campaign_id == campaign_id
            )
        ).all()
    )
```

- [ ] **Step 4: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_generation_templates.py -v`
Expected: PASS — 4 testes.

- [ ] **Step 5: Commit**

```bash
git add app/services/content/generation_templates.py test/services/test_content_generation_templates.py
git commit -m "feat(content): add generation-template CRUD service"
```

---

### Task 3: Endpoints de templates de geração

**Files:**
- Create: `app/controllers/v1/content/generation_templates.py`
- Modify: `app/router.py`

**Interfaces:**
- Consumes: `generation_templates.create_template`/`list_templates` (Task 2); `GenerationTemplateCreate`/`GenerationTemplateRead` (Task 1); `content_auth.verify_tenant_token`, `new_router` (`app/controllers/v1/base.py`), `audit.write_audit_log` — mesmos já usados em `app/controllers/v1/content/approval_rules.py`.
- Produces: `POST /content/campaigns/{campaign_id}/templates` (201), `GET /content/campaigns/{campaign_id}/templates`. Nenhuma task seguinte depende diretamente destes endpoints (o scheduler chama o serviço, não a API).

- [ ] **Step 1: Escrever o controller**

Crie `app/controllers/v1/content/generation_templates.py`, espelhando `app/controllers/v1/content/approval_rules.py`:

```python
from fastapi import Depends, HTTPException
from sqlmodel import Session

from app.controllers import content_auth
from app.controllers.v1.base import new_router
from app.db import get_session
from app.models.content import ContentTenant, GenerationTemplateCreate, GenerationTemplateRead
from app.services.content import audit
from app.services.content import generation_templates as templates_service

router = new_router(dependencies=[Depends(content_auth.verify_tenant_token)])


@router.post(
    "/content/campaigns/{campaign_id}/templates",
    response_model=GenerationTemplateRead,
    status_code=201,
)
def create_template(
    campaign_id: int,
    payload: GenerationTemplateCreate,
    session: Session = Depends(get_session),
    tenant: ContentTenant = Depends(content_auth.verify_tenant_token),
):
    if campaign_id != payload.campaign_id:
        raise HTTPException(
            status_code=422, detail="campaign_id in path and body must match"
        )
    template = templates_service.create_template(
        session,
        tenant_id=tenant.id,
        campaign_id=payload.campaign_id,
        type=payload.type,
        generation_prompt=payload.generation_prompt,
        avatar_id=payload.avatar_id,
        voice_id=payload.voice_id,
        is_synthetic_media=payload.is_synthetic_media,
        content_category=payload.content_category,
        aspect_ratio=payload.aspect_ratio,
        resolution=payload.resolution,
        duration=payload.duration,
    )
    if template is None:
        raise HTTPException(status_code=404, detail="Campaign not found")
    audit.write_audit_log(
        session,
        tenant_id=tenant.id,
        entity_type="generation_template",
        entity_id=template.id,
        action="created",
        actor=f"tenant:{tenant.id}",
    )
    return template


@router.get(
    "/content/campaigns/{campaign_id}/templates",
    response_model=list[GenerationTemplateRead],
)
def list_templates(
    campaign_id: int,
    session: Session = Depends(get_session),
    tenant: ContentTenant = Depends(content_auth.verify_tenant_token),
):
    return templates_service.list_templates(
        session, tenant_id=tenant.id, campaign_id=campaign_id
    )
```

- [ ] **Step 2: Registrar o router**

Em `app/router.py`, adicione `generation_templates` ao import de `app.controllers.v1.content` (ordem alfabética, entre `clients` e `models`) e registre o router junto dos outros de conteúdo:

```python
from app.controllers.v1.content import (
    approval_rules,
    avatars,
    campaigns,
    clients,
    generation_templates,
    models,
    pieces,
    providers,
    publications,
    social_accounts,
    tenants,
)
```

```python
root_api_router.include_router(generation_templates.router)
```

(adicione essa linha junto das outras `include_router` de conteúdo, por exemplo logo depois de `approval_rules.router`).

- [ ] **Step 3: Verificar que a aplicação sobe sem erro de rota duplicada/import**

Run: `python -c "from app.asgi import app; print([r.path for r in app.routes if 'templates' in r.path])"`
Expected: imprime `['/content/campaigns/{campaign_id}/templates', '/content/campaigns/{campaign_id}/templates']` (uma linha por método HTTP registrado), sem exceção.

- [ ] **Step 4: Commit**

```bash
git add app/controllers/v1/content/generation_templates.py app/router.py
git commit -m "feat(content): add generation-template endpoints"
```

---

### Task 4: Aprovar/rejeitar piece manualmente — serviço

**Files:**
- Modify: `app/services/content/pieces.py`
- Test: `test/services/test_content_pieces.py`

**Interfaces:**
- Consumes: `ContentPiece`, `ContentPieceStatus` (`app/models/content.py`); `get_piece` (já existe neste mesmo arquivo).
- Produces: `approve_piece(session, *, tenant_id, piece_id) -> Optional[ContentPiece]`, `reject_piece(session, *, tenant_id, piece_id) -> Optional[ContentPiece]`. Retornam `None` tanto quando a piece não existe/não pertence ao tenant quanto quando ela não está mais em `pending_approval` no momento da escrita — o controller (Task 5) distingue os dois casos com uma checagem prévia. Usadas pelo controller (Task 5).

- [ ] **Step 1: Escrever os testes falhos**

Adicione ao final de `test/services/test_content_pieces.py`:

```python
from datetime import datetime

from app.models.content import ContentPieceStatus


class TestApprovePiece(unittest.TestCase):
    def test_returns_none_when_piece_not_found(self):
        session = MagicMock()

        with patch.object(pieces_service, "get_piece", return_value=None):
            result = pieces_service.approve_piece(session, tenant_id=1, piece_id=99)

        self.assertIsNone(result)

    def test_approves_when_still_pending_approval(self):
        piece = MagicMock(id=10, status=ContentPieceStatus.pending_approval)
        session = MagicMock()
        session.exec.return_value.rowcount = 1

        with patch.object(pieces_service, "get_piece", return_value=piece):
            result = pieces_service.approve_piece(session, tenant_id=1, piece_id=10)

        self.assertIs(result, piece)
        session.commit.assert_called_once()
        session.refresh.assert_called_once_with(piece)

    def test_returns_none_when_status_changed_concurrently(self):
        piece = MagicMock(id=10, status=ContentPieceStatus.pending_approval)
        session = MagicMock()
        session.exec.return_value.rowcount = 0

        with patch.object(pieces_service, "get_piece", return_value=piece):
            result = pieces_service.approve_piece(session, tenant_id=1, piece_id=10)

        self.assertIsNone(result)
        session.refresh.assert_not_called()


class TestRejectPiece(unittest.TestCase):
    def test_rejects_when_still_pending_approval(self):
        piece = MagicMock(id=10, status=ContentPieceStatus.pending_approval)
        session = MagicMock()
        session.exec.return_value.rowcount = 1

        with patch.object(pieces_service, "get_piece", return_value=piece):
            result = pieces_service.reject_piece(session, tenant_id=1, piece_id=10)

        self.assertIs(result, piece)
        session.commit.assert_called_once()

    def test_returns_none_when_status_changed_concurrently(self):
        piece = MagicMock(id=10, status=ContentPieceStatus.pending_approval)
        session = MagicMock()
        session.exec.return_value.rowcount = 0

        with patch.object(pieces_service, "get_piece", return_value=piece):
            result = pieces_service.reject_piece(session, tenant_id=1, piece_id=10)

        self.assertIsNone(result)
```

- [ ] **Step 2: Rodar e confirmar falha**

Run: `pytest test/services/test_content_pieces.py -v -k "ApprovePiece or RejectPiece"`
Expected: FAIL — `AttributeError: module 'app.services.content.pieces' has no attribute 'approve_piece'`

- [ ] **Step 3: Implementar**

No topo de `app/services/content/pieces.py`, o import de `sqlmodel` já existe (`from sqlmodel import Session, select`) — troque por:

```python
from sqlmodel import Session, select, update
```

Adicione ao final do arquivo (depois de `create_piece`):

```python
def _conditional_transition(
    session: Session, *, piece_id: int, values: dict
) -> bool:
    """UPDATE guardado: só aplica se a piece ainda estiver pending_approval.

    Fecha a corrida entre uma decisão automática (approval_action, Passe 2 do
    scheduler) e uma ação manual do humano chegando ao mesmo tempo — quem
    escrever primeiro vence, o outro descarta sem sobrescrever. Retorna se a
    escrita realmente aconteceu.
    """
    result = session.exec(
        update(ContentPiece)
        .where(
            ContentPiece.id == piece_id,
            ContentPiece.status == ContentPieceStatus.pending_approval,
        )
        .values(**values)
    )
    session.commit()
    return result.rowcount > 0


def approve_piece(session: Session, *, tenant_id: int, piece_id: int) -> Optional[ContentPiece]:
    piece = get_piece(session, tenant_id=tenant_id, piece_id=piece_id)
    if piece is None:
        return None
    applied = _conditional_transition(
        session,
        piece_id=piece_id,
        values={
            "status": ContentPieceStatus.approved,
            "approved_at": datetime.utcnow(),
            "updated_at": datetime.utcnow(),
        },
    )
    if not applied:
        return None
    session.refresh(piece)
    return piece


def reject_piece(session: Session, *, tenant_id: int, piece_id: int) -> Optional[ContentPiece]:
    piece = get_piece(session, tenant_id=tenant_id, piece_id=piece_id)
    if piece is None:
        return None
    applied = _conditional_transition(
        session,
        piece_id=piece_id,
        values={
            "status": ContentPieceStatus.rejected,
            "updated_at": datetime.utcnow(),
        },
    )
    if not applied:
        return None
    session.refresh(piece)
    return piece
```

`ContentPieceStatus` já está importado neste arquivo (`from app.models.content import ContentPieceStatus, ContentPieceType`, adicionado quando `create_piece` foi escrito).

- [ ] **Step 4: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_pieces.py -v`
Expected: PASS — todos os testes do arquivo, incluindo os já existentes.

- [ ] **Step 5: Commit**

```bash
git add app/services/content/pieces.py test/services/test_content_pieces.py
git commit -m "feat(content): add approve/reject piece service with optimistic guard"
```

---

### Task 5: Aprovar/rejeitar piece manualmente — endpoints

**Files:**
- Modify: `app/controllers/v1/content/pieces.py`

**Interfaces:**
- Consumes: `pieces_service.approve_piece`/`reject_piece` (Task 4).
- Produces: `POST /content/pieces/{piece_id}/approve`, `POST /content/pieces/{piece_id}/reject`.

- [ ] **Step 1: Adicionar os endpoints**

Ao final de `app/controllers/v1/content/pieces.py`, adicione:

```python
@router.post("/content/pieces/{piece_id}/approve", response_model=ContentPieceRead)
def approve_piece(
    piece_id: int,
    session: Session = Depends(get_session),
    tenant: ContentTenant = Depends(content_auth.verify_tenant_token),
):
    piece = pieces_service.get_piece(session, tenant_id=tenant.id, piece_id=piece_id)
    if piece is None:
        raise HTTPException(status_code=404, detail="Content piece not found")

    updated = pieces_service.approve_piece(session, tenant_id=tenant.id, piece_id=piece_id)
    if updated is None:
        raise HTTPException(
            status_code=409,
            detail=f"Piece must be pending_approval to approve, got '{piece.status.value}'",
        )

    audit.write_audit_log(
        session,
        tenant_id=tenant.id,
        entity_type="content_piece",
        entity_id=piece_id,
        action="approved",
        actor=f"tenant:{tenant.id}",
    )
    return updated


@router.post("/content/pieces/{piece_id}/reject", response_model=ContentPieceRead)
def reject_piece(
    piece_id: int,
    session: Session = Depends(get_session),
    tenant: ContentTenant = Depends(content_auth.verify_tenant_token),
):
    piece = pieces_service.get_piece(session, tenant_id=tenant.id, piece_id=piece_id)
    if piece is None:
        raise HTTPException(status_code=404, detail="Content piece not found")

    updated = pieces_service.reject_piece(session, tenant_id=tenant.id, piece_id=piece_id)
    if updated is None:
        raise HTTPException(
            status_code=409,
            detail=f"Piece must be pending_approval to reject, got '{piece.status.value}'",
        )

    audit.write_audit_log(
        session,
        tenant_id=tenant.id,
        entity_type="content_piece",
        entity_id=piece_id,
        action="rejected",
        actor=f"tenant:{tenant.id}",
    )
    return updated
```

- [ ] **Step 2: Verificar que as rotas sobem**

Run: `python -c "from app.asgi import app; print(sorted(r.path for r in app.routes if 'approve' in r.path or 'reject' in r.path))"`
Expected: `['/content/pieces/{piece_id}/approve', '/content/pieces/{piece_id}/reject']`

- [ ] **Step 3: Commit**

```bash
git add app/controllers/v1/content/pieces.py
git commit -m "feat(content): add manual approve/reject endpoints"
```

---

### Task 6: automation_scheduler.py — Passe 1 (geração proativa)

**Files:**
- Create: `app/services/content/automation_scheduler.py`
- Test: `test/services/test_content_automation_scheduler.py`

**Interfaces:**
- Consumes: `ContentCampaign`, `ContentClient`, `ContentTenant`, `EntitlementStatus`, `ContentPiece`, `ContentPieceStatus`, `ContentPieceCreate` (`app/models/content.py`); `generation_templates.list_templates` (Task 2); `pieces_service.create_piece` (`app/services/content/pieces.py`, já existente — assinatura `create_piece(session, *, tenant_id, payload) -> tuple[ContentPiece, bool]`); `audit.write_audit_log`.
- Produces: `next_slot(max_scheduled: Optional[datetime], *, now: datetime) -> datetime`, `pick_template_index(autogenerated_count: int, template_count: int) -> int`, `_fill_campaign_calendars(session, *, batch_limit: int) -> None`. Consumidas pelo `_tick()` da Task 9.

- [ ] **Step 1: Escrever os testes falhos das funções puras**

Crie `test/services/test_content_automation_scheduler.py`:

```python
import unittest
from datetime import datetime, timedelta
from unittest.mock import MagicMock, patch

from app.services.content import automation_scheduler as scheduler


class TestNextSlot(unittest.TestCase):
    def test_no_existing_pieces_returns_now(self):
        now = datetime(2026, 8, 29, 10, 0, 0)

        result = scheduler.next_slot(None, now=now)

        self.assertEqual(result, now)

    def test_existing_max_scheduled_advances_by_one_day(self):
        max_scheduled = datetime(2026, 8, 30, 10, 0, 0)
        now = datetime(2026, 8, 29, 10, 0, 0)

        result = scheduler.next_slot(max_scheduled, now=now)

        self.assertEqual(result, max_scheduled + timedelta(days=1))


class TestPickTemplateIndex(unittest.TestCase):
    def test_rotates_through_templates_in_order(self):
        self.assertEqual(scheduler.pick_template_index(0, 3), 0)
        self.assertEqual(scheduler.pick_template_index(1, 3), 1)
        self.assertEqual(scheduler.pick_template_index(2, 3), 2)
        self.assertEqual(scheduler.pick_template_index(3, 3), 0)
        self.assertEqual(scheduler.pick_template_index(7, 3), 1)

    def test_single_template_always_picks_it(self):
        self.assertEqual(scheduler.pick_template_index(0, 1), 0)
        self.assertEqual(scheduler.pick_template_index(50, 1), 0)
```

- [ ] **Step 2: Rodar e confirmar falha**

Run: `pytest test/services/test_content_automation_scheduler.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.services.content.automation_scheduler'`

- [ ] **Step 3: Implementar as funções puras**

Crie `app/services/content/automation_scheduler.py`:

```python
import os
from datetime import datetime, timedelta
from typing import Optional

from loguru import logger
from sqlalchemy.exc import IntegrityError
from sqlmodel import Session, select

from app.db import get_engine
from app.models.content import (
    ContentCampaign,
    ContentClient,
    ContentPiece,
    ContentPieceCreate,
    ContentPieceStatus,
    ContentTenant,
    EntitlementStatus,
)
from app.services.content import audit
from app.services.content import generation_templates as templates_service
from app.services.content import pieces as pieces_service

TICK_SECONDS = float(os.environ.get("CONTENT_AUTOMATION_TICK_SECONDS", 300))
_BATCH_LIMIT = int(os.environ.get("CONTENT_AUTOMATION_BATCH_SIZE", 50))


def next_slot(max_scheduled: Optional[datetime], *, now: datetime) -> datetime:
    """The next calendar slot for a campaign's proactive generation.

    Pure and stateless: the caller supplies the current max `scheduled_for`
    among the campaign's non-terminal pieces (or None when there are none).
    """
    if max_scheduled is None:
        return now
    return max_scheduled + timedelta(days=1)


def pick_template_index(autogenerated_count: int, template_count: int) -> int:
    """Round-robin index into a campaign's template list.

    `template_count` must be >= 1 — callers already skip campaigns with no
    templates before reaching this function.
    """
    return autogenerated_count % template_count
```

- [ ] **Step 4: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_automation_scheduler.py -v`
Expected: PASS — 7 testes.

- [ ] **Step 5: Escrever os testes falhos de `_fill_campaign_calendars`**

Adicione a `test/services/test_content_automation_scheduler.py`:

```python
from app.models.content import ContentPieceType


class TestFillCampaignCalendars(unittest.TestCase):
    def _campaign(self, **overrides):
        base = dict(id=1, client_id=1, horizon_days=7, status="active")
        base.update(overrides)
        return MagicMock(**base)

    def _tenant(self, **overrides):
        base = dict(id=1, entitlement_status=EntitlementStatus.active)
        base.update(overrides)
        return MagicMock(**base)

    def _client(self, tenant_id=1):
        return MagicMock(id=1, tenant_id=tenant_id)

    def test_skips_campaign_with_no_templates(self):
        session = MagicMock()
        campaign = self._campaign()
        session.exec.return_value.all.side_effect = [[campaign], []]
        session.get.side_effect = lambda model, id_: {
            ("ContentClient", 1): self._client(),
            ("ContentTenant", 1): self._tenant(),
        }.get((model.__name__, id_))

        with patch.object(templates_service, "list_templates", return_value=[]):
            with patch.object(pieces_service, "create_piece") as create_piece:
                scheduler._fill_campaign_calendars(session, batch_limit=50)

        create_piece.assert_not_called()

    def test_skips_campaign_when_tenant_not_active(self):
        session = MagicMock()
        campaign = self._campaign()
        session.exec.return_value.all.side_effect = [[campaign], []]
        session.get.side_effect = lambda model, id_: {
            ("ContentClient", 1): self._client(),
            ("ContentTenant", 1): self._tenant(entitlement_status=EntitlementStatus.inactive),
        }.get((model.__name__, id_))

        with patch.object(pieces_service, "create_piece") as create_piece:
            scheduler._fill_campaign_calendars(session, batch_limit=50)

        create_piece.assert_not_called()

    def test_creates_one_piece_from_rotated_template_when_horizon_not_full(self):
        session = MagicMock()
        campaign = self._campaign(horizon_days=7)
        template = MagicMock(
            id=9,
            type=ContentPieceType.image,
            generation_prompt="a cat",
            avatar_id=None,
            voice_id=None,
            is_synthetic_media=False,
            content_category=None,
            aspect_ratio="9:16",
            resolution=None,
            duration=None,
        )
        # First query: active campaigns. Second: no existing non-terminal
        # pieces for max_scheduled. Third: 0 autogenerated pieces so far.
        session.exec.return_value.all.side_effect = [[campaign], [], []]
        session.exec.return_value.first.return_value = None
        session.get.side_effect = lambda model, id_: {
            ("ContentClient", 1): self._client(),
            ("ContentTenant", 1): self._tenant(),
        }.get((model.__name__, id_))
        created_piece = MagicMock(id=100)

        with patch.object(templates_service, "list_templates", return_value=[template]):
            with patch.object(
                pieces_service, "create_piece", return_value=(created_piece, True)
            ) as create_piece:
                scheduler._fill_campaign_calendars(session, batch_limit=50)

        create_piece.assert_called_once()
        _, kwargs = create_piece.call_args
        self.assertEqual(kwargs["tenant_id"], 1)
        payload = kwargs["payload"]
        self.assertEqual(payload.campaign_id, 1)
        self.assertEqual(payload.type, ContentPieceType.image)
        self.assertTrue(payload.idempotency_key.startswith("auto:1:"))
        self.assertTrue(created_piece.is_autogenerated)
        session.add.assert_any_call(created_piece)

    def test_integrity_error_on_one_campaign_does_not_stop_the_batch(self):
        session = MagicMock()
        campaign_a = self._campaign(id=1, client_id=1)
        campaign_b = self._campaign(id=2, client_id=1)
        template = MagicMock(
            id=9,
            type=ContentPieceType.image,
            generation_prompt="a cat",
            avatar_id=None,
            voice_id=None,
            is_synthetic_media=False,
            content_category=None,
            aspect_ratio="9:16",
            resolution=None,
            duration=None,
        )
        session.exec.return_value.all.side_effect = [
            [campaign_a, campaign_b], [], [], [], [],
        ]
        session.get.side_effect = lambda model, id_: {
            ("ContentClient", 1): self._client(),
            ("ContentTenant", 1): self._tenant(),
        }.get((model.__name__, id_))
        created_piece = MagicMock(id=101)

        with patch.object(templates_service, "list_templates", return_value=[template]):
            with patch.object(
                pieces_service,
                "create_piece",
                side_effect=[IntegrityError("stmt", {}, Exception("dup")), (created_piece, True)],
            ) as create_piece:
                scheduler._fill_campaign_calendars(session, batch_limit=50)

        self.assertEqual(create_piece.call_count, 2)
        session.rollback.assert_called_once()
```

- [ ] **Step 6: Rodar e confirmar falha**

Run: `pytest test/services/test_content_automation_scheduler.py -v -k FillCampaignCalendars`
Expected: FAIL — `AttributeError: module 'app.services.content.automation_scheduler' has no attribute '_fill_campaign_calendars'`

- [ ] **Step 7: Implementar `_fill_campaign_calendars`**

Adicione ao final de `app/services/content/automation_scheduler.py`:

```python
def _tenant_for_campaign(session: Session, campaign: ContentCampaign) -> Optional[ContentTenant]:
    client = session.get(ContentClient, campaign.client_id)
    if client is None:
        return None
    return session.get(ContentTenant, client.tenant_id)


def _fill_campaign_calendars(session: Session, *, batch_limit: int) -> None:
    """Passe 1: cria, no máximo, uma piece por campanha elegível neste tick.

    Preenchimento gradual — não o horizonte inteiro de uma vez — para não
    gerar um pico de trabalho de geração. Uma campanha nova leva vários ticks
    até o calendário ficar cheio; comportamento pretendido, não um bug.
    """
    campaigns = session.exec(
        select(ContentCampaign)
        .where(ContentCampaign.status == "active")
        .limit(batch_limit)
    ).all()

    for campaign in campaigns:
        tenant = _tenant_for_campaign(session, campaign)
        if tenant is None or tenant.entitlement_status != EntitlementStatus.active:
            continue

        templates = templates_service.list_templates(
            session, tenant_id=tenant.id, campaign_id=campaign.id
        )
        if not templates:
            continue

        max_scheduled = session.exec(
            select(ContentPiece.scheduled_for)
            .where(
                ContentPiece.campaign_id == campaign.id,
                ContentPiece.status.not_in(
                    [ContentPieceStatus.rejected, ContentPieceStatus.failed]
                ),
            )
            .order_by(ContentPiece.scheduled_for.desc())
        ).first()
        slot = next_slot(max_scheduled, now=datetime.utcnow())
        if slot >= datetime.utcnow() + timedelta(days=campaign.horizon_days):
            continue

        autogenerated_count = len(
            session.exec(
                select(ContentPiece.id).where(
                    ContentPiece.campaign_id == campaign.id,
                    ContentPiece.is_autogenerated == True,  # noqa: E712
                )
            ).all()
        )
        template = templates[pick_template_index(autogenerated_count, len(templates))]

        piece_create = ContentPieceCreate(
            campaign_id=campaign.id,
            type=template.type,
            idempotency_key=f"auto:{campaign.id}:{slot.date().isoformat()}",
            is_synthetic_media=template.is_synthetic_media,
            generation_prompt=template.generation_prompt,
            avatar_id=template.avatar_id,
            voice_id=template.voice_id,
            content_category=template.content_category,
            aspect_ratio=template.aspect_ratio,
            resolution=template.resolution,
            duration=template.duration,
        )
        try:
            piece, created = pieces_service.create_piece(
                session, tenant_id=tenant.id, payload=piece_create
            )
        except IntegrityError:
            # Outra réplica já criou a piece para este campaign_id + slot
            # (mesma UniqueConstraint que resolve_publication_request usa na
            # fase 3) — corrida normal entre réplicas, não um erro.
            session.rollback()
            logger.debug(
                f"campaign {campaign.id}: concurrent auto-generation for slot "
                f"{slot.date().isoformat()} — another replica won the race"
            )
            continue

        if not created:
            continue

        piece.scheduled_for = slot
        piece.is_autogenerated = True
        session.add(piece)
        session.commit()
        audit.write_audit_log(
            session,
            tenant_id=tenant.id,
            entity_type="content_piece",
            entity_id=piece.id,
            action="autogenerated",
            actor="system:automation_scheduler",
        )
```

- [ ] **Step 8: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_automation_scheduler.py -v`
Expected: PASS — todos os testes do arquivo.

- [ ] **Step 9: Commit**

```bash
git add app/services/content/automation_scheduler.py test/services/test_content_automation_scheduler.py
git commit -m "feat(content): add proactive generation pass (Passe 1) to automation scheduler"
```

---

### Task 7: automation_scheduler.py — Passe 2 (avaliação de aprovação)

**Files:**
- Modify: `app/services/content/automation_scheduler.py`
- Test: `test/services/test_content_automation_scheduler.py`

**Interfaces:**
- Consumes: `ApprovalAction`, `ContentCategory`, `RiskLevel` (`app/models/content.py`); `approval_rules_service.list_approval_rules(session, *, tenant_id, campaign_id) -> List[ContentApprovalRule]` (já existente, ordenado por `priority` desc).
- Produces: `rule_matches(condition: dict, *, content_category: Optional[ContentCategory], risk_level: RiskLevel) -> bool`, `_evaluate_pending_approvals(session, *, batch_limit: int) -> None`. Consumida pelo `_tick()` da Task 9.

- [ ] **Step 1: Escrever os testes falhos de `rule_matches`**

Adicione a `test/services/test_content_automation_scheduler.py`:

```python
from app.models.content import ContentCategory, RiskLevel


class TestRuleMatches(unittest.TestCase):
    def test_empty_condition_matches_anything(self):
        self.assertTrue(
            scheduler.rule_matches(
                {}, content_category=ContentCategory.medical, risk_level=RiskLevel.high
            )
        )
        self.assertTrue(
            scheduler.rule_matches({}, content_category=None, risk_level=RiskLevel.none)
        )

    def test_matches_when_risk_level_in_allowed_list(self):
        condition = {"risk_level": ["high", "medium"]}

        self.assertTrue(
            scheduler.rule_matches(
                condition, content_category=None, risk_level=RiskLevel.high
            )
        )
        self.assertFalse(
            scheduler.rule_matches(
                condition, content_category=None, risk_level=RiskLevel.low
            )
        )

    def test_matches_when_content_category_in_allowed_list(self):
        condition = {"content_category": ["financial", "insurance"]}

        self.assertTrue(
            scheduler.rule_matches(
                condition, content_category=ContentCategory.financial, risk_level=RiskLevel.none
            )
        )
        self.assertFalse(
            scheduler.rule_matches(
                condition, content_category=None, risk_level=RiskLevel.none
            )
        )

    def test_multiple_keys_require_all_to_match(self):
        condition = {"content_category": ["financial"], "risk_level": ["high"]}

        self.assertTrue(
            scheduler.rule_matches(
                condition, content_category=ContentCategory.financial, risk_level=RiskLevel.high
            )
        )
        self.assertFalse(
            scheduler.rule_matches(
                condition, content_category=ContentCategory.financial, risk_level=RiskLevel.medium
            )
        )

    def test_unsupported_condition_key_never_matches(self):
        condition = {"type": ["video"]}

        self.assertFalse(
            scheduler.rule_matches(
                condition, content_category=None, risk_level=RiskLevel.none
            )
        )
```

- [ ] **Step 2: Rodar e confirmar falha**

Run: `pytest test/services/test_content_automation_scheduler.py -v -k RuleMatches`
Expected: FAIL — `AttributeError: module 'app.services.content.automation_scheduler' has no attribute 'rule_matches'`

- [ ] **Step 3: Implementar `rule_matches`**

Adicione ao final de `app/services/content/automation_scheduler.py` (os imports de `ApprovalAction`, `ContentCategory`, `RiskLevel` vão para o bloco de import de `app.models.content` já existente no topo do arquivo):

```python
_SUPPORTED_CONDITION_KEYS = {"content_category", "risk_level"}


def rule_matches(
    condition: dict, *, content_category: Optional["ContentCategory"], risk_level: "RiskLevel"
) -> bool:
    """A rule's `condition` matches a piece's fields.

    Every key present in `condition` must match — AND semantics, not OR. An
    empty condition matches anything (catch-all fallback rule). An
    unsupported key (only content_category/risk_level are wired) never
    matches: a typo'd or future key must fall through to require_review, not
    silently match everything.
    """
    piece_values = {
        "content_category": content_category.value if content_category else None,
        "risk_level": risk_level.value,
    }
    for key, allowed_values in condition.items():
        if key not in _SUPPORTED_CONDITION_KEYS:
            return False
        if piece_values[key] not in allowed_values:
            return False
    return True
```

Atualize o import de `app.models.content` no topo do arquivo para incluir `ApprovalAction`, `ContentApprovalRule` (usado só para tipagem, opcional), `ContentCategory`, `RiskLevel`:

```python
from app.models.content import (
    ApprovalAction,
    ContentCampaign,
    ContentCategory,
    ContentClient,
    ContentPiece,
    ContentPieceCreate,
    ContentPieceStatus,
    ContentTenant,
    EntitlementStatus,
    RiskLevel,
)
```

(remova as aspas em `"ContentCategory"`/`"RiskLevel"` na assinatura de `rule_matches` agora que o import direto existe — ficam como `Optional[ContentCategory]`/`risk_level: RiskLevel`.)

- [ ] **Step 4: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_automation_scheduler.py -v -k RuleMatches`
Expected: PASS — 5 testes.

- [ ] **Step 5: Escrever os testes falhos de `_evaluate_pending_approvals`**

Adicione a `test/services/test_content_automation_scheduler.py`:

```python
from app.services.content import approval_rules as approval_rules_service


class TestEvaluatePendingApprovals(unittest.TestCase):
    def _piece(self, **overrides):
        base = dict(
            id=1,
            campaign_id=1,
            status=ContentPieceStatus.pending_approval,
            requires_human_review=False,
            content_category=None,
            risk_level=RiskLevel.none,
        )
        base.update(overrides)
        return MagicMock(**base)

    def _client_and_tenant(self):
        return MagicMock(id=1, tenant_id=1), MagicMock(id=1)

    def test_requires_human_review_always_wins_over_rules(self):
        piece = self._piece(requires_human_review=True)
        session = MagicMock()
        session.exec.return_value.all.return_value = [piece]
        session.exec.return_value.rowcount = 1
        client, _ = self._client_and_tenant()
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): client,
        }.get((model.__name__, id_))

        with patch.object(approval_rules_service, "list_approval_rules") as list_rules:
            scheduler._evaluate_pending_approvals(session, batch_limit=50)

        list_rules.assert_not_called()
        update_call = session.exec.call_args_list[-1].args[0]
        compiled_values = update_call._values if hasattr(update_call, "_values") else None
        # Sanity: an UPDATE statement was issued (compiled check covered by
        # the dispatcher's own precedent — assert on outcome via commit here).
        session.commit.assert_called()

    def test_matching_rule_auto_approves(self):
        piece = self._piece(risk_level=RiskLevel.high)
        rule = MagicMock(
            condition={"risk_level": ["high"]}, action=ApprovalAction.auto_approve, priority=10
        )
        session = MagicMock()
        session.exec.return_value.all.return_value = [piece]
        session.exec.return_value.rowcount = 1
        client, _ = self._client_and_tenant()
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): client,
        }.get((model.__name__, id_))

        with patch.object(approval_rules_service, "list_approval_rules", return_value=[rule]):
            scheduler._evaluate_pending_approvals(session, batch_limit=50)

        session.commit.assert_called()

    def test_no_matching_rule_defaults_to_require_review(self):
        piece = self._piece()
        rule = MagicMock(
            condition={"risk_level": ["high"]}, action=ApprovalAction.auto_approve, priority=10
        )
        session = MagicMock()
        session.exec.return_value.all.return_value = [piece]
        session.exec.return_value.rowcount = 1
        client, _ = self._client_and_tenant()
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): client,
        }.get((model.__name__, id_))

        with patch.object(approval_rules_service, "list_approval_rules", return_value=[rule]):
            scheduler._evaluate_pending_approvals(session, batch_limit=50)

        session.commit.assert_called()

    def test_concurrent_status_change_drops_the_decision_without_error(self):
        piece = self._piece()
        session = MagicMock()
        session.exec.return_value.all.return_value = [piece]
        session.exec.return_value.rowcount = 0
        client, _ = self._client_and_tenant()
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): client,
        }.get((model.__name__, id_))

        with patch.object(approval_rules_service, "list_approval_rules", return_value=[]):
            scheduler._evaluate_pending_approvals(session, batch_limit=50)
```

- [ ] **Step 6: Rodar e confirmar falha**

Run: `pytest test/services/test_content_automation_scheduler.py -v -k EvaluatePendingApprovals`
Expected: FAIL — `AttributeError: module 'app.services.content.automation_scheduler' has no attribute '_evaluate_pending_approvals'`

- [ ] **Step 7: Implementar `_evaluate_pending_approvals`**

Adicione ao final de `app/services/content/automation_scheduler.py` (importe `update` de `sqlmodel` e `approval_rules` de `app.services.content`):

```python
from sqlmodel import Session, select, update  # substitui a linha de import existente

from app.services.content import approval_rules as approval_rules_service
```

```python
def _decide_approval_action(
    session: Session, piece: ContentPiece
) -> tuple[ApprovalAction, bool]:
    """Retorna (approval_action, auto_approved)."""
    if piece.requires_human_review:
        return ApprovalAction.require_review, False

    campaign = session.get(ContentCampaign, piece.campaign_id)
    client = session.get(ContentClient, campaign.client_id)
    rules = approval_rules_service.list_approval_rules(
        session, tenant_id=client.tenant_id, campaign_id=piece.campaign_id
    )
    for rule in rules:
        if rule_matches(
            rule.condition, content_category=piece.content_category, risk_level=piece.risk_level
        ):
            return rule.action, rule.action == ApprovalAction.auto_approve
    return ApprovalAction.require_review, False


def _evaluate_pending_approvals(session: Session, *, batch_limit: int) -> None:
    """Passe 2: decide auto_approve/require_review para pieces ainda não avaliadas.

    Sem lock: avaliar a mesma piece duas vezes numa corrida rara entre
    réplicas é determinístico e sem efeito colateral externo. A guarda que
    importa é a escrita condicional abaixo, que protege contra uma corrida
    real (o motor decidindo ao mesmo tempo que um humano rejeita a piece).
    """
    pieces = session.exec(
        select(ContentPiece)
        .where(
            ContentPiece.status == ContentPieceStatus.pending_approval,
            ContentPiece.approval_action.is_(None),
        )
        .limit(batch_limit)
    ).all()

    for piece in pieces:
        action, auto_approved = _decide_approval_action(session, piece)
        values = {"approval_action": action, "updated_at": datetime.utcnow()}
        if auto_approved:
            values["status"] = ContentPieceStatus.approved
            values["approved_at"] = datetime.utcnow()

        result = session.exec(
            update(ContentPiece)
            .where(
                ContentPiece.id == piece.id,
                ContentPiece.status == ContentPieceStatus.pending_approval,
            )
            .values(**values)
        )
        session.commit()
        if result.rowcount == 0:
            logger.debug(
                f"piece {piece.id} status changed concurrently — dropping "
                f"approval-engine decision"
            )
            continue

        campaign = session.get(ContentCampaign, piece.campaign_id)
        client = session.get(ContentClient, campaign.client_id)
        audit.write_audit_log(
            session,
            tenant_id=client.tenant_id,
            entity_type="content_piece",
            entity_id=piece.id,
            action=f"approval_action:{action.value}",
            actor="system:approval_engine",
        )
```

- [ ] **Step 8: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_automation_scheduler.py -v`
Expected: PASS — todos os testes do arquivo.

- [ ] **Step 9: Commit**

```bash
git add app/services/content/automation_scheduler.py test/services/test_content_automation_scheduler.py
git commit -m "feat(content): add approval-rule evaluation pass (Passe 2) to automation scheduler"
```

---

### Task 8: automation_scheduler.py — Passe 3 (disparo da publicação agendada)

**Files:**
- Modify: `app/services/content/automation_scheduler.py`
- Test: `test/services/test_content_automation_scheduler.py`

**Interfaces:**
- Consumes: `ContentSocialAccount`, `ContentSocialPublication` (`app/models/content.py`, `app/models/content_publishing.py`); `publications_service.resolve_publication_request(session, *, piece, social_account_ids) -> Tuple[List[ContentSocialPublication], List[dict]]` (fase 3, já existente, inalterada).
- Produces: `_dispatch_scheduled_publications(session, *, batch_limit: int) -> None`. Consumida pelo `_tick()` da Task 9.

- [ ] **Step 1: Escrever os testes falhos**

Adicione a `test/services/test_content_automation_scheduler.py`:

```python
from app.models.content import ContentSocialAccount
from app.services.content import publications as publications_service


class TestDispatchScheduledPublications(unittest.TestCase):
    def test_skips_when_tenant_not_active(self):
        piece = MagicMock(id=1, campaign_id=1)
        session = MagicMock()
        session.exec.return_value.all.side_effect = [[piece], []]
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): MagicMock(id=1, tenant_id=1),
            ("ContentTenant", 1): MagicMock(id=1, entitlement_status=EntitlementStatus.inactive),
        }.get((model.__name__, id_))

        with patch.object(publications_service, "resolve_publication_request") as resolve:
            scheduler._dispatch_scheduled_publications(session, batch_limit=50)

        resolve.assert_not_called()

    def test_resolves_publication_for_all_active_accounts_of_the_client(self):
        piece = MagicMock(id=1, campaign_id=1)
        accounts = [MagicMock(id=10), MagicMock(id=11)]
        session = MagicMock()
        session.exec.return_value.all.side_effect = [[piece], accounts]
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): MagicMock(id=1, tenant_id=1),
            ("ContentTenant", 1): MagicMock(id=1, entitlement_status=EntitlementStatus.active),
        }.get((model.__name__, id_))

        with patch.object(
            publications_service, "resolve_publication_request", return_value=([], [])
        ) as resolve:
            scheduler._dispatch_scheduled_publications(session, batch_limit=50)

        resolve.assert_called_once()
        _, kwargs = resolve.call_args
        self.assertIs(kwargs["piece"], piece)
        self.assertEqual(kwargs["social_account_ids"], [10, 11])

    def test_integrity_error_on_one_piece_does_not_stop_the_batch(self):
        piece_a = MagicMock(id=1, campaign_id=1)
        piece_b = MagicMock(id=2, campaign_id=1)
        accounts = [MagicMock(id=10)]
        session = MagicMock()
        session.exec.return_value.all.side_effect = [[piece_a, piece_b], accounts, accounts]
        session.get.side_effect = lambda model, id_: {
            ("ContentCampaign", 1): MagicMock(id=1, client_id=1),
            ("ContentClient", 1): MagicMock(id=1, tenant_id=1),
            ("ContentTenant", 1): MagicMock(id=1, entitlement_status=EntitlementStatus.active),
        }.get((model.__name__, id_))

        with patch.object(
            publications_service,
            "resolve_publication_request",
            side_effect=[IntegrityError("stmt", {}, Exception("dup")), ([], [])],
        ) as resolve:
            scheduler._dispatch_scheduled_publications(session, batch_limit=50)

        self.assertEqual(resolve.call_count, 2)
        session.rollback.assert_called_once()
```

- [ ] **Step 2: Rodar e confirmar falha**

Run: `pytest test/services/test_content_automation_scheduler.py -v -k DispatchScheduledPublications`
Expected: FAIL — `AttributeError: module 'app.services.content.automation_scheduler' has no attribute '_dispatch_scheduled_publications'`

- [ ] **Step 3: Implementar**

Adicione o import de `ContentSocialAccount` ao bloco de import de `app.models.content`, e `publications` a `app.services.content`, no topo de `app/services/content/automation_scheduler.py`:

```python
from app.models.content import (
    ApprovalAction,
    ContentCampaign,
    ContentCategory,
    ContentClient,
    ContentPiece,
    ContentPieceCreate,
    ContentPieceStatus,
    ContentSocialAccount,
    ContentTenant,
    EntitlementStatus,
    RiskLevel,
)
from app.models.content_publishing import ContentSocialPublication
from app.services.content import approval_rules as approval_rules_service
from app.services.content import publications as publications_service
```

Adicione ao final do arquivo:

```python
def _dispatch_scheduled_publications(session: Session, *, batch_limit: int) -> None:
    """Passe 3: dispara resolve_publication_request uma vez por piece devida.

    O NOT EXISTS é a fronteira entre este motor (S4) e o executor da fase 3
    (S3): depois que a primeira linha de content_social_publications existe
    para uma piece, o ciclo de vida inteiro (retry, sucesso, falha) já é
    responsabilidade só da fase 3 — este passe nunca mais toca essa piece.
    """
    now = datetime.utcnow()
    due_piece_ids_subquery = (
        select(ContentSocialPublication.content_piece_id).distinct()
    )
    pieces = session.exec(
        select(ContentPiece)
        .where(
            ContentPiece.status == ContentPieceStatus.approved,
            ContentPiece.scheduled_for.is_not(None),
            ContentPiece.scheduled_for <= now,
            ContentPiece.id.not_in(due_piece_ids_subquery),
        )
        .limit(batch_limit)
    ).all()

    for piece in pieces:
        campaign = session.get(ContentCampaign, piece.campaign_id)
        client = session.get(ContentClient, campaign.client_id)
        tenant = session.get(ContentTenant, client.tenant_id)
        if tenant is None or tenant.entitlement_status != EntitlementStatus.active:
            continue

        accounts = session.exec(
            select(ContentSocialAccount).where(
                ContentSocialAccount.client_id == client.id,
                ContentSocialAccount.status == "active",
            )
        ).all()
        if not accounts:
            continue

        try:
            publications_service.resolve_publication_request(
                session, piece=piece, social_account_ids=[account.id for account in accounts]
            )
        except IntegrityError:
            # Outra réplica já enfileirou este par piece/conta (mesma
            # UniqueConstraint(content_piece_id, social_account_id) que a
            # fase 3 já protege) — corrida normal entre réplicas.
            session.rollback()
            logger.debug(
                f"piece {piece.id}: concurrent scheduled-publish dispatch — "
                f"another replica won the race"
            )
```

- [ ] **Step 4: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_automation_scheduler.py -v`
Expected: PASS — todos os testes do arquivo.

- [ ] **Step 5: Commit**

```bash
git add app/services/content/automation_scheduler.py test/services/test_content_automation_scheduler.py
git commit -m "feat(content): add scheduled-publish dispatch pass (Passe 3) to automation scheduler"
```

---

### Task 9: automation_scheduler.py — lifecycle e wiring

**Files:**
- Modify: `app/services/content/automation_scheduler.py`
- Modify: `app/asgi.py`
- Modify: `config.example.toml`
- Test: `test/services/test_content_automation_scheduler.py`

**Interfaces:**
- Consumes: `_fill_campaign_calendars`, `_evaluate_pending_approvals`, `_dispatch_scheduled_publications` (Tasks 6-8).
- Produces: `start_scheduler() -> None`, `stop_scheduler() -> None`. Chamadas por `app/asgi.py`, mesmo padrão de `start_dispatcher`/`stop_dispatcher` da fase 3.

- [ ] **Step 1: Escrever o teste falho do lifecycle**

Adicione a `test/services/test_content_automation_scheduler.py`:

```python
import time


class TestSchedulerLifecycle(unittest.TestCase):
    def tearDown(self):
        scheduler.stop_scheduler()

    def test_tick_calls_all_three_passes_in_order(self):
        calls = []
        with patch.object(
            scheduler, "_fill_campaign_calendars", side_effect=lambda **_: calls.append("gen")
        ):
            with patch.object(
                scheduler,
                "_evaluate_pending_approvals",
                side_effect=lambda **_: calls.append("approve"),
            ):
                with patch.object(
                    scheduler,
                    "_dispatch_scheduled_publications",
                    side_effect=lambda **_: calls.append("publish"),
                ):
                    with patch.object(scheduler, "Session"):
                        scheduler._tick()

        self.assertEqual(calls, ["gen", "approve", "publish"])

    def test_start_then_stop_is_safe_and_idempotent(self):
        scheduler.start_scheduler()
        time.sleep(0.05)
        scheduler.stop_scheduler()
        # Segundo start/stop não deve levantar exceção.
        scheduler.start_scheduler()
        scheduler.stop_scheduler()
```

- [ ] **Step 2: Rodar e confirmar falha**

Run: `pytest test/services/test_content_automation_scheduler.py -v -k SchedulerLifecycle`
Expected: FAIL — `AttributeError: module 'app.services.content.automation_scheduler' has no attribute '_tick'`

- [ ] **Step 3: Implementar o lifecycle**

Adicione ao final de `app/services/content/automation_scheduler.py`:

```python
import threading

_stop_event = threading.Event()
_scheduler_thread: Optional[threading.Thread] = None


def _tick() -> None:
    """Um tick roda os 3 passes em sequência, cada um em sua própria sessão.

    Ao contrário do dispatcher de publicação da fase 3, nenhum passe aqui
    precisa de um pool de threads próprio: a geração já é assíncrona por
    conta de pieces_service.create_piece/schedule_piece (fase 2), e a
    avaliação de regra e o disparo de publicação agendada são operações de
    banco rápidas — o trabalho pesado sempre acontece em outro lugar.
    """
    with Session(get_engine()) as session:
        _fill_campaign_calendars(session, batch_limit=_BATCH_LIMIT)
    with Session(get_engine()) as session:
        _evaluate_pending_approvals(session, batch_limit=_BATCH_LIMIT)
    with Session(get_engine()) as session:
        _dispatch_scheduled_publications(session, batch_limit=_BATCH_LIMIT)


def _loop() -> None:
    while not _stop_event.is_set():
        try:
            _tick()
        except Exception:
            logger.exception("automation scheduler tick failed")
        _stop_event.wait(TICK_SECONDS)


def start_scheduler() -> None:
    global _scheduler_thread
    if _scheduler_thread is not None:
        return
    _stop_event.clear()
    _scheduler_thread = threading.Thread(
        target=_loop, name="mpt-content-automation-scheduler", daemon=True
    )
    _scheduler_thread.start()


def stop_scheduler() -> None:
    global _scheduler_thread
    _stop_event.set()
    if _scheduler_thread is not None:
        _scheduler_thread.join(timeout=5)
        _scheduler_thread = None
```

Note que `threading` já deve estar importado uma única vez no topo do arquivo (mova o `import threading` do meio do arquivo para o bloco de imports no topo, junto de `import os`).

- [ ] **Step 4: Rodar e confirmar sucesso**

Run: `pytest test/services/test_content_automation_scheduler.py -v`
Expected: PASS — suíte inteira do arquivo.

- [ ] **Step 5: Conectar ao lifespan da aplicação**

Em `app/asgi.py`, depois do bloco que inicia o dispatcher de publicação (`start_dispatcher()`/`logger.info("content publish dispatcher started")`) e antes de `configured_api_key = ...`, adicione:

```python
    from app.services.content.automation_scheduler import start_scheduler, stop_scheduler

    start_scheduler()
    logger.info("content automation scheduler started")
```

No bloco `finally`, depois de `stop_dispatcher()`/`logger.info("content publish dispatcher stopped")` e antes de `logger.info("shutdown event")`, adicione:

```python
        stop_scheduler()
        logger.info("content automation scheduler stopped")
```

- [ ] **Step 6: Verificar que a aplicação sobe e desce sem erro**

Run: `python -c "
import asyncio
from app.asgi import app, application_lifespan
async def check():
    async with application_lifespan(app):
        pass
asyncio.run(check())
print('lifespan OK')
"`
Expected: imprime `lifespan OK` sem exceção (o teardown do `async with` já chama o bloco `finally`, incluindo `stop_scheduler()`).

- [ ] **Step 7: Documentar a variável de ambiente**

Em `config.example.toml`, no bloco de comentários "Content module — social publishing (sub-project 3)" perto do final do arquivo, adicione logo abaixo:

```toml
# Content module — automation and approval (sub-project 4).
# These are secrets and must be set as environment variables, never here:
#   CONTENT_AUTOMATION_TICK_SECONDS=300          # optional, scheduler poll interval
#   CONTENT_AUTOMATION_BATCH_SIZE=50              # optional, campaigns/pieces processed per tick per pass
```

- [ ] **Step 8: Rodar a suíte completa do módulo de conteúdo**

Run: `pytest test/services/ -v -k content`
Expected: PASS — nenhuma regressão nos testes das fases 1-3.

- [ ] **Step 9: Commit**

```bash
git add app/services/content/automation_scheduler.py app/asgi.py config.example.toml test/services/test_content_automation_scheduler.py
git commit -m "feat(content): wire automation scheduler lifecycle into the application lifespan"
```
